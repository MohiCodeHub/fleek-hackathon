# Base URL of the chat-app API (the Render service). Leave EMPTY for local dev —
# the Vite proxy forwards /api to http://localhost:4000.
# On Vercel, set this to your Render URL, e.g. https://seller-chat.onrender.com
VITE_API_BASE_URL=
