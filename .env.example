# OpenAI
OPENAI_API_KEY=sk-...

# Model IDs (override if needed)
MODEL_REASONING=gpt-4o
MODEL_FAST=gpt-4o-mini

# Wassist (WhatsApp transport) — needed only for M3+ (real WhatsApp)
WASSIST_API_KEY=
WASSIST_BASE_URL=https://alpha-plagiocephalic-darrell.ngrok-free.dev
WASSIST_WEBHOOK_SECRET=
# Public URL where Wassist will POST inbound messages (e.g. your ngrok https URL + /webhook)
PUBLIC_WEBHOOK_URL=

# Local server
PORT=8787

# SQLite location
DB_PATH=data/jackjill.db
